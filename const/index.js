const swaggerDocPath = '/v2/api-docs';
const swaggerUIPath = '/swagger-ui.html';
const swaggerBasePath = '#!/';

const globalPrefix = 'swagger_ui_';

module.exports = {
  swaggerDocPath,
  swaggerUIPath,
  swaggerBasePath,
  globalPrefix
};
